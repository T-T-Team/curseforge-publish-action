{
  "name": "curseforge-publish-action",
  "version": "2.1.1",
  "description": "Action for publishing mods to CurseForge",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/index.js",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@actions/core": "2.0.3"
  },
  "keywords": [],
  "author": "Toma",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}